service: cart
team: checkout
environment: checkout-staging

replicas: 2

# Staging runs closer to production sizing than dev does.
resources:
  requests: { cpu: 100m, memory: 128Mi }
  limits:   { cpu: 400m, memory: 256Mi }
