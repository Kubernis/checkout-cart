# Deploying to an environment is adding a file like this one. No platform
# repository is touched; the ApplicationSet watches this directory.
#
# `environment` must match an environment that already exists in the catalog —
# it selects the ArgoCD project and the namespace, both of which were created
# when the tenant was provisioned.
service: cart
team: checkout
environment: checkout-dev

replicas: 2
